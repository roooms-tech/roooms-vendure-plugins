{
  "name": "roooms-vendure-plugins",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prettier": "prettier --ignore-path .gitignore '**/*.{ts,tsx,mjs,js,json,yml}' --write"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "~4.7.4",
    "prettier": "^2.7.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
